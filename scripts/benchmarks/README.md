# Benchmarks

Small developer-only performance probes for workbook rebuild behavior.

- `monthly_parse.py` — measures the monthly dataset/read/derive path.
- `monthly_formula_metrics.py` — reports formula/populated-cell counts and workbook size for `main_monthly`.

These scripts are not application entry points and are not part of the production workflow.
