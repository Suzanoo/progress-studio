# Windows Packaging

Reserved for the Windows production build configuration.

Before adding an installer definition, verify:

- the wheel contains all JSON configuration and dashboard icon resources;
- `progress-studio` and `progress-studio-cli` resolve to the stable package entry points;
- Create Progress / Rebuild smoke tests pass from a clean environment.
